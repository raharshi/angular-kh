import { CustomTileCasePipe } from './custom-tile-case.pipe';

describe('CustomTileCasePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomTileCasePipe();
    expect(pipe).toBeTruthy();
  });
});
